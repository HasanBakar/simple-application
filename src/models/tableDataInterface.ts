export interface TableDataInterface {
  name: {
    common: string,
    official: string,
  };
  capital: [string],
  subregion: string,
  startOfWeek: string,
  region: string,
  population: number,
}
